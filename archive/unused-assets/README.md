# Unused assets (2026-06-28)

Page-specific CSS/JS moved here after public route cleanup. These files were only referenced by archived pages in `archive/removed-pages/` and are not loaded by `/`, `/simulateur/`, or `/sur-plan/`.

| File | Former page |
|------|-------------|
| `stylesheets/gallery.css`, `javascripts/gallery.js` | `/gallery/` |
| `stylesheets/about.css`, `javascripts/about.js` | `/about/` |
| `stylesheets/location.css`, `javascripts/location.js` | `/location/` |
| `stylesheets/infrastructure.css`, `javascripts/infrastructure.js` | `/infrastructure/` (replaced by `sur-plan.css` / `sur-plan.js`) |

Images and media under `assets/` were **not** moved — many are still referenced by homepage sections and `/sur-plan/`.
